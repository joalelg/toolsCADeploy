name: heroku-jupyter
dependencies:
  # bind notebook version (compatibility to pgcontents)
  #- notebook = 4
  #- ipywidgets
  #- requests
  # bind prince version (for CA)
  - prince==0.6.3
  - pip:
    - pgcontents
    - git+git://github.com/ipython-contrib/jupyter_contrib_nbextensions.git
